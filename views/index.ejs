<!DOCTYPE html>
<html lang="en">
	<head>
		<% include partials/head %>
	</head>
	<body class="container">
		<header>
			<% include partials/header %>
		</header>

		<main>
			<div class="row">
			<% include partials/error %>
			<% if ( user_id == undefined ) { %>
				<div class="col-sm-8">
					<div class="jumbotron">
						<h1>You are not signed in</h1>
						<p>Please use the login form to sign in for access to this service.</p>
					</div>
			<% } else { %>
				<div class="col-sm-12">
					<div class="jumbotron">
						<h1>Signed in! :-)</h1>
						<p>Thanks for signing in, please enjoy, <%= user.name %></p>
					</div>
			<% } %>
				</div>
			<% if ( user_id == undefined ) { %>
				<% include partials/login %>
			<% } %>
			</div>
		<% if ( user_id !== undefined ) { %>
			<% include partials/tasks %>
		<% } %>
		</main>

    <footer>
        <% include partials/footer %>
    </footer>

</body>
</html>
