<!DOCTYPE html>
<html lang="en">
<head>
    <% include partials/head %>
</head>
<body class="container">

    <header>
        <% include partials/header %>
    </header>

    <main>
        <div class="row">
          <% include partials/error %>
          <% if ( !valid ) { %>
            <div class="col-sm-12">
              <div class="alert alert-success" role="alert"><strong>Not signed ion!</strong> please sign in!</div>
            </div>
            <% include partials/login %>
          <% } else { %>
            <div class="col-sm-12">
          <% } %>
                <div class="jumbotron">
                    <img class="pull-right img-circle" src="<%= user.avatar %>" height="96" width="96"/>
                    <span class="h1"><%= user.name %></span>
                    <p>
                      <% if ( teacher.name !== user.name ) { %><strong>Teacher:</strong> <em><%= teacher.name %> of</em><% } else { %> Teacher at <% } %> <em><%= school.name %><br></em>
                      <strong>Challenges Attempted:</strong> <%= user.tasksDone.length %><br>
                      <strong>Challenges Due:</strong> <%= school.tasks.length %><br>
                      <strong>Score:</strong> <%= user.score %><br>
                      <strong>Grade:</strong> <%= user.grade %>
                    </p>
                </div>
            </div>
          </div>
        </div>
    </main>

    <footer>
        <% include partials/footer %>
    </footer>

</body>
</html>
